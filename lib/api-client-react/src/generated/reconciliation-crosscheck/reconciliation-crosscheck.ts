/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * Wildflower Fundraising CRM API (Airtable-aligned schema)
 * OpenAPI spec version: 0.2.0
 */
import {
  useQuery
} from '@tanstack/react-query';
import type {
  QueryFunction,
  QueryKey,
  UseQueryOptions,
  UseQueryResult
} from '@tanstack/react-query';

import type {
  ForbiddenResponse,
  GetReconciliationCrosscheckParams,
  ReconciliationCrosscheckResult
} from '../api.schemas';

import { customFetch } from '../../custom-fetch';
import type { ErrorType } from '../../custom-fetch';

type AwaitedInput<T> = PromiseLike<T> | T;

      type Awaited<O> = O extends AwaitedInput<infer T> ? T : never;


type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];



/**
 * @summary Admin-only, strictly READ-ONLY. Cross-checks three historical transaction spreadsheet exports (Stripe/Donorbox donations, raw Stripe charges, and a QuickBooks Statement of Activity) against the CRM's already-synced money records, classifying every spreadsheet row as matched / amount_mismatch / missing. Never mints, imports, or modifies any gift, staged payment, charge, or payout — it only reports gaps. Returns the (filtered, paginated) rows plus a per-source summary and an aggregate month-by-month gap breakdown computed over the FULL dataset.
 */
export const getGetReconciliationCrosscheckUrl = (params?: GetReconciliationCrosscheckParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/reconciliation-crosscheck?${stringifiedParams}` : `/api/reconciliation-crosscheck`
}

export const getReconciliationCrosscheck = async (params?: GetReconciliationCrosscheckParams, options?: RequestInit): Promise<ReconciliationCrosscheckResult> => {
  
  return customFetch<ReconciliationCrosscheckResult>(getGetReconciliationCrosscheckUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}
  




export const getGetReconciliationCrosscheckQueryKey = (params?: GetReconciliationCrosscheckParams,) => {
    return [
    `/api/reconciliation-crosscheck`, ...(params ? [params] : [])
    ] as const;
    }

    
export const getGetReconciliationCrosscheckQueryOptions = <TData = Awaited<ReturnType<typeof getReconciliationCrosscheck>>, TError = ErrorType<ForbiddenResponse>>(params?: GetReconciliationCrosscheckParams, options?: { query?:UseQueryOptions<Awaited<ReturnType<typeof getReconciliationCrosscheck>>, TError, TData>, request?: SecondParameter<typeof customFetch>}
) => {

const {query: queryOptions, request: requestOptions} = options ?? {};

  const queryKey =  queryOptions?.queryKey ?? getGetReconciliationCrosscheckQueryKey(params);

  

    const queryFn: QueryFunction<Awaited<ReturnType<typeof getReconciliationCrosscheck>>> = ({ signal }) => getReconciliationCrosscheck(params, { signal, ...requestOptions });

      

      

   return  { queryKey, queryFn, ...queryOptions} as UseQueryOptions<Awaited<ReturnType<typeof getReconciliationCrosscheck>>, TError, TData> & { queryKey: QueryKey }
}

export type GetReconciliationCrosscheckQueryResult = NonNullable<Awaited<ReturnType<typeof getReconciliationCrosscheck>>>
export type GetReconciliationCrosscheckQueryError = ErrorType<ForbiddenResponse>


/**
 * @summary Admin-only, strictly READ-ONLY. Cross-checks three historical transaction spreadsheet exports (Stripe/Donorbox donations, raw Stripe charges, and a QuickBooks Statement of Activity) against the CRM's already-synced money records, classifying every spreadsheet row as matched / amount_mismatch / missing. Never mints, imports, or modifies any gift, staged payment, charge, or payout — it only reports gaps. Returns the (filtered, paginated) rows plus a per-source summary and an aggregate month-by-month gap breakdown computed over the FULL dataset.
 */

export function useGetReconciliationCrosscheck<TData = Awaited<ReturnType<typeof getReconciliationCrosscheck>>, TError = ErrorType<ForbiddenResponse>>(
 params?: GetReconciliationCrosscheckParams, options?: { query?:UseQueryOptions<Awaited<ReturnType<typeof getReconciliationCrosscheck>>, TError, TData>, request?: SecondParameter<typeof customFetch>}
  
 ):  UseQueryResult<TData, TError> & { queryKey: QueryKey } {

  const queryOptions = getGetReconciliationCrosscheckQueryOptions(params,options)

  const query = useQuery(queryOptions) as  UseQueryResult<TData, TError> & { queryKey: QueryKey };

  return { ...query, queryKey: queryOptions.queryKey };
}




