Simplify the Wildflower Fundraising CRM feedback system. The user has decided that implementation proposals generated when feedback is submitted are unreliable because they are not grounded in the current code state. Implement this change in the development workspace only; do not publish or modify production.

First inspect the actual current source, schema, migrations, OpenAPI contract, generated clients, tests, and feedback UI. Then:

1. Keep each feedback record as the original user-authored feedback only: category/type (suggestion, problem, question, etc.), message, source page/path, reporter identity, submitted timestamp, private screenshot/storage reference, ordinary feedback status, and admin notes. Preserve all existing feedback rows and screenshots.
2. Remove the entire implementation-proposal feature from the UI: the Proposal column/status, Implementation proposal section, version display, Implementation brief, Start implementation button, Modify proposal button, and any other proposal-generation or implementation-dispatch controls. The feedback detail should remain a compact review view with the issue/question, screenshot, status, and admin notes.
3. Remove backend/API code that creates, regenerates, stores, returns, or dispatches proposals/implementation briefs. New feedback submission must not call an LLM or enqueue/run proposal generation. Remove unused proposal endpoints, services, background jobs, prompts, queues, and tests. Do not replace them with new fields or generated summaries.
4. Remove all database columns/tables used only for proposals or implementation dispatch. Inspect exact names and usages before editing. Add a straightforward development migration that drops only those unused proposal-related fields/tables; do not drop original feedback, screenshot, status, or admin-note data. No backfill or zero-downtime compatibility layer is needed. Do not copy proposal content anywhere.
5. Update the Drizzle schema, OpenAPI specification, generated clients/Zod types through the normal generator, frontend queries/types, and tests so nothing reads or populates removed fields.
6. Verify: creating feedback persists the original fields and screenshot reference without any proposal work; the queue has no Proposal column; the detail has no proposal/brief/version/implementation buttons; status/admin notes still save; existing screenshots still display; typecheck, focused tests, OpenAPI/codegen consistency, and production build pass.

Report the exact removed UI elements, code paths, jobs/endpoints, and database fields/tables. Also report the migration SQL and test/build results. Do not commit, push, run a production migration, edit production data, or publish.